[pytest]
markers =
    slow: full stress-scenario runs (media generation + whisper); deselected by default
    e2e: studio browser E2E (playwright + real exports); deselected by default
addopts = -m "not slow and not e2e"
